<header class="header">
    <div class="logo">
        <img src="{{data.logo.[0].image.src}}" alt="{{data.logo.[0].alt}}">
    </div>
    <nav>
        {{#each data.navigation.[0].items}}
            <a href="{{this.href}}">{{this.text}}</a>
        {{/each}}
    </nav>
</header>
